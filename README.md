# Introduction

This is a Python function wrapper for MySQL based on mysql-connector 

# Preliminary

* Required Package:
    * mysql-connector:
    ```
    $ pip install mysql-connector
    ```

# Usage
